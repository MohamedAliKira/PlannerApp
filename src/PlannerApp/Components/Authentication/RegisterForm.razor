@namespace PlannerApp.Components

<EditForm Model="_model" OnValidSubmit="RegisterUserAsync">
    <FluentValidationValidator />

    <MudCard>
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Typo="Typo.h6">@Language["WelcomeTo"] Planner App v2.0</MudText>
                @*<MudText Typo="Typo.body2">Create a new account</MudText>*@
            </CardHeaderContent>
        </MudCardHeader>
        <MudCardContent>
            <MudTextField Label="@Language["FirstName"]" HelperText="@Language["Required"]" @bind-Value="@_model.FirstName" />
            <MudTextField Label="@Language["LastName"]" HelperText="@Language["Required"]" @bind-Value="@_model.LastName" />
            <MudTextField Label="@Language["Email"]" HelperText="@Language["Required"]" @bind-Value="@_model.Email" />
            <MudTextField InputType="InputType.Password" Label="@Language["Password"]" HelperText="@Language["Required"]" @bind-Value="@_model.Password" />
            <MudTextField InputType="InputType.Password" Label="@Language["ConfirmPassword"]" HelperText="@Language["Required"]" @bind-Value="@_model.ConfirmPassword" />
            
            <br />
            <ValidationSummary />

            @if (!string.IsNullOrWhiteSpace(_errorMessage))
            {
                <MudAlert Severity="Severity.Error" Class="my-2" Variant="Variant.Filled">
                    @_errorMessage
                </MudAlert>
            }            
        </MudCardContent>

        <MudCardActions>
            <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Primary" Disabled="_isBusy">@Language["Register"]</MudButton>
            <MudButton Variant="Variant.Outlined" Color="Color.Secondary" Class="ml-1" OnClick="RedirectToLogin" Disabled="_isBusy">@Language["Login"]</MudButton>
        </MudCardActions>
    </MudCard>

</EditForm>